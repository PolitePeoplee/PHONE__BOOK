//﻿#include <windows.h>
#include <iostream>
#include <fstream>
#include <string> 
#include <cassert>
#include <vector>
#include <cstring>
vector<int> found(string find_name);
void showuserdata(vector<int> nums);
bool only_digits(const char* phone_number);
bool only_letters(const char* name);
